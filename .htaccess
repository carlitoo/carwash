#<IfModule mod_rewrite.c>
#    Options +FollowSymLinks
#    RewriteEngine on
#    RewriteBase /
#    RewriteRule ^$ app/webroot/ [L]
#    RewriteRule (.*) app/webroot/$1 [L]
#</IfModule>
<IfModule mod_rewrite.c>
   RewriteEngine on
   RewriteRule    ^$ app/webroot/    [L]
   RewriteRule    (.*) app/webroot/$1 [L]
</IfModule>